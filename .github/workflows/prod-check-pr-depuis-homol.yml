name: 🔒 Vérification PR homol → main

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]
    branches:
      - main

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  check-pr-source:
    name: 🔒 Vérification PR homol → main
    uses: LeZouzouEnWeb/workflows/.github/workflows/check-pr-source.yml@V1.0.5
    permissions:
      contents: read
      issues: write
      pull-requests: write
    with:
      source_branch: homol
      target_branch: main
